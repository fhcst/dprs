# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.pytest_cache/

# --- Environments ---
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# --- IDEs ---
# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains (PyCharm, etc.)
.idea/

# --- OS Specific ---
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp

# Linux
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# --- Databases / Logs ---
*.log
*.sqlite
*.sql
*.db

# --- Node.js / Frontend ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.npm
.eslintcache
.stylelintcache
.next
.nuxt
.svelte-kit
dist/
out/
build/
.cache/

# --- Other common ignores ---
*.swp
*.bak
*.tmp
*.temp
.DS_Store
*.seed
*.pid
*.pid.lock
.spectra/
openspec/.vector-search.db*

# --- Rust build artifacts (keep pkg/ — pre-built WASM bundle for frontend) ---
crates/*/target/

# --- Tailwind Standalone CLI binary ---
tools/tailwindcss

# --- Tailwind CSS build output ---
src/static/css/tailwind.css

# --- MongoDB 備份產物（scripts/backup.sh / mongo-backup service 輸出）---
backups/

# --- 防呆：禁止提交會被「自動合併」的 docker-compose.override.yml ---
# 本專案的本機 dev overlay 是「不自動合併、需顯式 -f」的 docker-compose.dev.yml（已進 repo）。
# compose 會在每個無 -f 的 up 自動合併同名的 docker-compose.override.yml（與 profile 無關），
# 連 `--profile tunnel up` 都會被它重新曝露 host port，故一律 gitignore 防止誤入 repo。
docker-compose.override.yml

# --- AI Agent local settings ---
.claude/settings.local.json
.superpowers